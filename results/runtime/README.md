# Runtime logs

These logs record historical scaling and Mennell runs. Their embedded input and
output paths reflect the repository layout at the time of measurement. The
large generated timing instances and tours are intentionally omitted; they can
be regenerated with the scripts in [`tools/data`](../../tools/data).
