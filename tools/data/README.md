# Data utilities

Run these scripts from the repository root. Conversion scripts read and write
the checked-in Mennell directories under `data/mennell`; visualizers, generated
instances, and ad hoc comparisons write to the ignored `output/` directory.

Install their Python dependencies with:

```powershell
python -m pip install -r experiments/requirements.txt
```
